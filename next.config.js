/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  reactStrictMode: true,
  basePath: "/game_list",
  assetPrefix: "/game_list",
  trailingSlash: true,
};

module.exports = nextConfig;
